version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - rvm install "ruby-2.6.10"
        - rvm use 2.6.10
        - bundle install
    build:
      commands:
        - bundle exec jekyll b
  artifacts:
    baseDirectory: _site
    files:
      - '**/*'
  cache:
    paths: []