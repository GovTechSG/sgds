version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - rvm use 2.6.6
        - bundle install
    build:
      commands:
        - bundle exec jekyll b
  artifacts:
    baseDirectory: _site
    files:
      - '**/*'
  cache:
    paths: []

