_site
.sass-cache
.jekyll-cache
.jekyll-metadata
.idea
.vscode
.images/
*.tar.gz

node_modules

/scripts
/apps/build
# Dont commit compressed images
/assets/img

# /css, /fonts and /js are distribution files
# they are committed for travis to build from the dev branch
# they are deployed at the gh-pages branch
# /css
# /fonts
# /js